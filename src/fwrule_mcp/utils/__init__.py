"""
Utility modules — cross-cutting concerns shared across all layers.

Modules:
  - validation.py : Input validation helpers (payload format checks, size checks)
  - limits.py     : Resource limit constants (max rule count, max payload size, etc.)
"""
